{
  "name": "obsidian-query-control",
  "version": "0.7.19",
  "description": "Adds extra controls for embedded query, search, and backlinks results.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "node esbuild.config.mjs production",
    "lint": "eslint \"src/**/*.ts\"",
    "postinstall": "chmod +x scripts/tag-version.sh",
    "release": "node scripts/updateManifestVersion.js && git add manifest.json && git commit --amend --no-edit && pnpm run tag-version",
    "release:patch": "pnpm version patch -m \"chore(release): %s\" && node scripts/updateManifestVersion.js && git add manifest.json && git commit --amend --no-edit && pnpm run tag-version",
    "release:minor": "pnpm version minor -m \"chore(release): %s\" && node scripts/updateManifestVersion.js && git add manifest.json && git commit --amend --no-edit && pnpm run tag-version",
    "tag-version": "./scripts/tag-version.sh"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.8.7",
    "@typescript-eslint/eslint-plugin": "^8.13.0",
    "@typescript-eslint/parser": "^8.13.0",
    "eslint": "^8.57.1",
    "esbuild": "^0.25.0",
    "monkey-around": "^3.0.0",
    "obsidian": "^1.6.6",
    "tslib": "^2.8.0",
    "typescript": "^5.6.3"
  }
}
